import './bootstrap';

//SSE example vanilla js
/*
const eventSource = new EventSource('/notifications/stream');
eventSource.addEventListener('notification', (event) => {
    console.log(event.data);
});
*/

console.log('app.js loaded');

